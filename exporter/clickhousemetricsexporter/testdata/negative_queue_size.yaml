receivers:
    nop:
  
processors:
    nop:
 
exporters:
    clickhousemetricswrite:
        endpoint: "localhost:8888"
        sending_queue:
            queue_size: -1
            num_consumers: 10

service:
    pipelines:
        metrics:
            receivers: [nop]
            processors: [nop]
            exporters: [clickhousemetricswrite]
    
    
